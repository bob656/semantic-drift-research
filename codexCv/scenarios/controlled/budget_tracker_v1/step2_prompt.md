Add monthly alerts.

Requirements:

- Add `is_over_budget()` and `get_alert_message()`.
- Preserve rollover semantics.
- Preserve the existing balance sign convention.

Return complete Python code only.
