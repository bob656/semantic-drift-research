Add export support.

Requirements:

- Add `export_monthly_report()` returning a serializable dictionary.
- Do not redesign `add_expense(category, amount)`.
- Existing rollover and balance semantics must remain unchanged.

Return complete Python code only.
