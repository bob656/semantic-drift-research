Add category summaries.

Requirements:

- Add `get_category_summary()` returning total spent per category.
- Keep `add_expense(category, amount)` callable as-is.
- Existing budget semantics must remain unchanged.

Return complete Python code only.
