Implement a `BudgetTracker` in Python.

Requirements:

- Track a monthly budget amount.
- Implement `add_expense(category, amount)` to record expenses.
- Implement `get_total_spent()`, `get_remaining_budget()`, and `close_month()`.
- Unused budget should roll into the next month.
- Overspending must not silently create positive remaining budget.

Return complete Python code only.
