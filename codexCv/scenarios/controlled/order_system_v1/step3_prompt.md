Add inventory reservation.

Requirements:

- Add `reserve_inventory(order_id)` that marks inventory as reserved for each item.
- Confirmed orders can reserve inventory.
- Do not redesign the external `create_order(customer, items)` API.
- Do not change cancellation semantics.

Return complete Python code only.
