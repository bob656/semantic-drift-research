# 실험 결과 분석: pilot_20260325_161738

## 실험 메타데이터

| 항목 | 값 |
|---|---|
| 일시 | 2026-03-25 16:17:38 |
| 모델 | gemma3:12b |
| Ollama 서버 | http://192.168.100.52:11434 |
| 반복 횟수 | 3회 |
| 평가 방식 | **exec** (코드 실행 테스트 — 첫 번째 실행) |
| 적용된 개선 | 충돌 유발형 시나리오(OrderSystem) + temperature=0.5 + CodeExecutor |

---

## 요약 통계

| 지표 | Baseline | StateDoc |
|---|---|---|
| 평균 드리프트 | **10.0점** | **10.0점** |
| 표준편차 | 0.0 | 0.0 |
| 개선 효과 | 0.000점 | |
| p-value | 1.0 → 통계적 검정 불가 | |

---

## 단계별 점수

| 회차 | 초기 | 수정1 | 수정2 | 수정3 | 수정4 | 드리프트 |
|---|---|---|---|---|---|---|
| Baseline Run 1 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |
| Baseline Run 2 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |
| Baseline Run 3 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |
| StateDoc Run 1 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |
| StateDoc Run 2 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |
| StateDoc Run 3 | 10.0 | 8.0 | **0.0** | **0.0** | **0.0** | 10.0 |

---

## 실제 원인: 테스트 코드 버그

### 결과가 보여주는 것

6회 전체 실행이 완전히 동일한 패턴 `[10.0, 8.0, 0.0, 0.0, 0.0]`. 두 에이전트, 3회 반복 모두 동일 → **에이전트 차이가 아닌 테스트 코드의 구조적 오류**.

### 원인 분석

수정1 요청: `total` 파라미터를 제거하고 items 합계로 자동계산하도록 변경

LLM은 지시에 따라 `add_order(order_id, items)` — total 없는 시그니처로 변경함.

**그런데 수정1 이후 테스트들이 여전히 `add_order(1, items, None)` (3인수)로 호출:**

```
Step 1 테스트:
  [결과 0] item_class_exists → True
  [결과 1] item_name → True
  [결과 2] item_price → True
  [결과 3] item_quantity → True
  add_order(1, [item1, item2], None) → TypeError! (인수 4개, 3개 기대)
  except → ("step1_exception", False)
  최종: 4/5 = 80% = 8.0점 ✓

Step 2 테스트:
  try 블록 첫 줄: add_order(1, [item1], None) → 즉시 TypeError
  results = [] 상태에서 except → ("step2_exception", False)
  최종: 0/1 = 0% = 0.0점 ✓
```

### 수정 조치

모든 테스트 파일(`_TESTS_STEP1` ~ `_TESTS_STEP4`)에 래퍼 함수 추가:

```python
def _add(om, order_id, items):
    """add_order가 total 파라미터를 받든 안 받든 동작하는 래퍼"""
    try:
        om.add_order(order_id, items)       # total 없는 시그니처 (수정1 이후)
    except TypeError:
        om.add_order(order_id, items, None) # total 있는 시그니처 (하위 호환)
```

---

## 이번 실험의 수확

### ✅ exec 평가가 처음으로 작동함

- 초기 단계(step 0): 4/4 테스트 통과 → 10.0점
- LLM이 정말로 `add_order`, `get_order`, `cancel_order`, `list_orders`를 정확히 구현했음이 확인됨
- 수정1(step 1): Item 클래스와 속성 접근이 올바르게 구현됨 (item_class_exists, item_name, item_price, item_quantity 모두 통과)

### ✅ temperature=0.5 효과

이전 실험들(temperature=0.1)과 달리 실행 시간이 더 일정하지 않아 LLM 출력에 다양성이 있음을 시사합니다.

### ❌ 테스트 코드가 LLM의 시그니처 변경을 수용하지 못함

이것은 `exec` 평가의 내재적 어려움: LLM이 요청에 따라 시그니처를 변경하면 테스트가 깨짐.
해결책: 래퍼 함수(`_add`)로 두 가지 시그니처 모두 허용.

---

## 실험 이력 비교

| 실험 | 평가 방식 | Baseline | StateDoc | 개선 | 비고 |
|---|---|---|---|---|---|
| 163921 | LLM | 1.96% | 0.0% | +1.96% | qwen2.5-coder:7b |
| 174711 | LLM | 0.0% | 0.0% | NaN | NaN 버그 |
| 104926 | LLM | 0.0% | 0.0% | 0.0% | NaN 수정 후 |
| 122254 | LLM | 0.0% | 1.75% | -1.75% | 비율 공식 왜곡 |
| 142427 | LLM | 0.167점 | 0.167점 | 0.000점 | 절대량 공식 |
| **161738** | **exec** | **10.0점** | **10.0점** | **0.000점** | **테스트 버그 (수정완료)** |

---

## 다음 실험

테스트 코드 수정 후 재실행:

```bash
python main.py --host http://192.168.100.52:11434 --eval-mode exec
```

수정1 이후 `add_order` 시그니처 변경을 테스트가 올바르게 처리하므로
수정2+에서 실제 기능 보존 여부가 측정될 것으로 기대됩니다.
